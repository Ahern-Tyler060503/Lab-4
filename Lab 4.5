# Tyler Ahern
# 9-27-21
# The program will calculate store and employee bonuses

# Lab 4-5
# The main function
def main():
    monthlySales = getSales() 

# This function gets the monthly sales    
def getSales():
    monthlySales = float(input('Enter the monthly sales $'))
    return monthlySales
    salesIncrease = getIncrease(salesIncrease)

# This function gets the percent of increease in sales
def getIncrease(salesIncrease):
    salesIncrease = float(input('Enter the percent of sales increase: '))
    salesIncrease = float(salesIncrease)
    salesIncrease = salesIncrease / 100
    return salesIncrease
    storeAmount = storeBonus(monthlySales)
    empAmount = empBonus(salesIncrease)
    printBonus(storeAmount, empAmount)

# This function determines the storeAmount bonus
def storeBonus(monthlySales):
    if monthlySales >= 110000:
        storeAmount = 6000
    elif monthlySales >= 100000:
        storeAmount =  5000
    elif monthlySales >= 90000:
        storeAmount = 4000
    elif monthlySales >= 80000:
        storeAmount = 3000
    else:
        storeAmount = 0
    return storeAmount        

# This function determines the empAmount bonus
def empBonus(salesIncrease):
    if salesIncrease >= 5:
        empAmount = 75
    if salesIncrease >= 4:
        empAmount = 50
    if salesIncrease >= 3:
        empAmount = 40
    else:
        empAmount = 0
    return empAmount

# This function prints the bonus information
def printBonus(storeAmount, empAmount):
    print ('The store bonus amount is $'), storeAmount
    print ('The employee bonus amount is $'), empAmount
    if storeAmount == 6000 and empAmount == 75:
        print('Congrats! You have reached the hiighest bonus amounts possible!')


# calls main
main()
